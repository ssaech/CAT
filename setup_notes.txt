python3 -m venv .venv
source .venv/bin/activate
python3 -m pip install -r requirements.txt

download the wav files, update paths in data_kaldi.sh
then
    cd /Users/web/CAT/egs/IuMien

    /Users/web/CAT/.venv/bin/python local/split_data.py \
    --data_path /Users/web/MightLJSpeech/MightLJSpeech-1.1/metadata.csv \
    --data_output_path /Users/web/MightLJSpeech/MightLJSpeech-1.1

    /Users/web/CAT/.venv/bin/python local/fliter_data.py

    /Users/web/CAT/.venv/bin/python local/extract_fbank.py \
    /Users/web/MightLJSpeech/MightLJSpeech-1.1

    /Users/web/CAT/.venv/bin/python utils/data/resolvedata.py


python utils/pipeline/asr.py exp/Mono-subword --start_stage 1 --stop_stage 4 


- The Iu Mien recipe is now in much better working shape locally: we repaired the data path, tokenizer path, and preprocessing flow, but full CAT ASR training still cannot run on this machine because CAT hard-requires CUDA and rejects CPU-only training in [coreutils.py](/Users/web/CAT/cat/shared/coreutils.py:497).

- Kaldi is not required for the local CPU workflow we set up. We avoided the repo’s Kaldi-dependent training route and instead used local preprocessing plus a separate CPU-friendly Hugging Face `Wav2Vec2 CTC` baseline.

- The source MightLJSpeech corpus only provided `metadata.csv`, not `train_data.txt`, `dev_data.txt`, or `test_data.txt`, so we generated those split files with the patched [split_data.py](/Users/web/CAT/egs/IuMien/local/split_data.py).

- We updated [fliter_data.py](/Users/web/CAT/egs/IuMien/local/fliter_data.py) so duplicate filtering works against the real local corpus path and safely ignores blank or malformed lines.

- We updated [extract_fbank.py](/Users/web/CAT/egs/IuMien/local/extract_fbank.py) so feature extraction handles the generated split files more robustly and does not fail on empty or malformed entries.

- We patched [data_prep.py](/Users/web/CAT/cat/utils/data/data_prep.py) so CAT can fall back to standard WAV loading without `torchcodec`, and we forced single-worker extraction to avoid the macOS `torch_shm_manager` shared-memory failure.

- We fixed the `Mono-subword` tokenizer output path in [hyper-p.json](/Users/web/CAT/egs/IuMien/exp/Mono-subword/hyper-p.json), which is why CAT stage 1 and stage 2 were finally able to run, even though full CAT training still cannot continue on CPU.

- Whisper on CPU was tested as a possible local transcription fallback, but it was not usable zero-shot for this dataset: it hallucinated Chinese output for an Iu Mien clip, so it is not a reliable path here.

- To keep moving without CUDA, we built a separate CPU-friendly supervised baseline in [train_wav2vec2_ctc_cpu.py](/Users/web/CAT/egs/IuMien/local/train_wav2vec2_ctc_cpu.py). That script reads the generated split files, builds a character vocabulary from Iu Mien transcripts, resamples audio to 16 kHz, trains on CPU, and saves checkpoints plus metrics.

- The tiny Wav2Vec2 smoke test succeeded, which confirmed that a local CPU training path is viable on this machine.

- The first `facebook/wav2vec2-base-960h` fine-tuning runs failed with `NaN` losses. We stabilized that path by lowering the learning rate, setting `grad-accum=1`, disabling spec augment by default, and adding an immediate failure check for non-finite loss in [train_wav2vec2_ctc_cpu.py](/Users/web/CAT/egs/IuMien/local/train_wav2vec2_ctc_cpu.py).

- The model load warnings were investigated and are expected rather than fatal:
  `wav2vec2.masked_spec_embed | MISSING` means a training-time parameter was initialized fresh,
  and `lm_head.weight` / `lm_head.bias | MISMATCH` means the CTC output layer was rebuilt for the Iu Mien label set instead of reusing the pretrained English output head.

- After stabilization, training became numerically healthy and losses decreased across runs and epochs, but `CER` and `WER` remained at `1.0`, so we added sample prediction logging to evaluation.

- That evaluation patch revealed the actual current failure mode: `dev_empty_pred_frac: 1.0`. The model is collapsing to CTC blank predictions, so all decoded hypotheses are empty even though the loss is improving.

- The current recommended next step is an unfrozen low-learning-rate run so the encoder has a better chance to escape blank-only decoding:
  `python local/train_wav2vec2_ctc_cpu.py --data-root /Users/web/MightLJSpeech/MightLJSpeech-1.1 --model facebook/wav2vec2-base-960h --save-dir exp_cpu/iumien_wav2vec2_unfrozen_200x5 --epochs 5 --max-train 200 --max-dev 50 --batch-size 1 --grad-accum 1 --lr 5e-6 --show-samples 5`
  and do not add `--freeze-feature-encoder`.



--- blank hyp
Here’s a short final status note you can use:

- The Iu Mien local recipe and preprocessing flow are now functional, but full CAT ASR training still cannot run on this machine because CAT requires CUDA and does not support CPU-only training in [coreutils.py](/Users/web/CAT/cat/shared/coreutils.py:497).

- We worked around the original dataset/setup issues by generating `train_data.txt`, `dev_data.txt`, and `test_data.txt` from `metadata.csv`, fixing local filtering and feature-prep scripts, and patching CAT’s audio/data prep so local preprocessing could run successfully on macOS.

- Kaldi was not required for the CPU path we tested. We instead built a separate CPU-friendly supervised baseline in [train_wav2vec2_ctc_cpu.py](/Users/web/CAT/egs/IuMien/local/train_wav2vec2_ctc_cpu.py) using Hugging Face `Wav2Vec2 CTC`.

- Whisper on CPU was tested as a possible zero-shot fallback, but it was not usable for this dataset because it hallucinated Chinese output for an Iu Mien clip.

- The first `facebook/wav2vec2-base-960h` fine-tuning attempts failed with `NaN` losses. We stabilized training by lowering the learning rate, setting `grad-accum=1`, disabling spec augment by default, and adding fast failure on non-finite loss.

- After those fixes, CPU training became numerically stable and losses decreased across runs and epochs, which confirmed that the local training loop itself was working.

- However, evaluation showed that the model collapsed to blank CTC predictions: `dev_empty_pred_frac: 1.0`, `dev_cer: 1.0`, and `dev_wer: 1.0`, with all decoded hypotheses empty.

- We also patched the script to support `--load-from` and `--eval-only` so saved checkpoints could be inspected directly; this confirmed that the saved `exp_cpu/iumien_wav2vec2_unfrozen_200x5/best_model` checkpoint is also blank-collapsed.

- Final result: the current CPU-friendly `facebook/wav2vec2-base-960h` setup is technically stable on this dataset, but it does not produce usable Iu Mien transcriptions because it converges to blank predictions.

- Recommended next step: stop iterating on this checkpoint and try a different pretrained model, ideally a smaller or more multilingual speech model, rather than continuing to train this one.

- facebook/mms-300m is now the preferred checkpoint because it is the first model that avoided both of the earlier failure modes: unlike facebook/wav2vec2-base-960h, it did not collapse to blank predictions, and unlike facebook/wav2vec2-xls-r-300m, it achieved better loss and error metrics on the same 1-epoch 100/20 CPU experiment. In your runs, mms-300m produced lower train_loss, lower dev_loss, lower dev_cer, slightly better dev_wer, and non-empty sample hypotheses, which makes it the strongest candidate so far for continued Iu Mien fine-tuning on this machine.

rule of thumb:
    dev_empty_pred_frac = 0.0 means the model is at least speaking instead of blanking
    dev_cer < 0.8 means it may be worth spot-testing on your own voice
    dev_cer < 0.5 means it is starting to become meaningfully testable
    dev_cer < 0.3 is where I’d start expecting occasional useful transcriptions on in-domain speech

Checkpoint before real testing
- The two MMS results came from the same pretrained backbone, facebook/mms-300m, but from different fresh fine-tuning runs rather than a continued checkpoint. In both cases the Iu Mien CTC output head was initialized from scratch, and early CTC training on a small dataset is fragile enough that one fresh run can start producing non-empty text while another drifts back toward blank predictions. The runs also used different train/dev subset sizes (100/20 versus 200/50), so the metrics are not directly comparable. The practical takeaway is that the better mms300m_debug result reflects a more favorable fine-tuning trajectory, which made resuming from that saved checkpoint the right next experiment to try instead of repeatedly restarting from the base pretrained model.
- Very small continuation runs are not rescuing the model. Repeated 1e-7 resume training on the same 100-example setup is gradually degrading the only checkpoint that showed real promise.
- The observed results indicate progressive deterioration rather than improvement: blank predictions become more common, decoded outputs become shorter and more collapsed, dev_cer worsens, and overall transcription quality declines even when loss decreases slightly.
- In plain terms, continuing this exact training recipe is hurting more than it is helping. At this stage, the current continuation path behaves more like decay than refinement.
- The checkpoint to preserve is exp_cpu/iumien_mms300m_debug/best_model, which remains the strongest non-blank model produced so far. Later continuation runs have not surpassed it and instead pushed the model back toward weaker behavior.
- The current plan is to stop this continuation chain, keep mms300m_debug/best_model as the best available checkpoint, and shift to inference on real recordings to judge practical usefulness. If training resumes later, the recipe should change more substantially rather than continue with more tiny learning-rate updates.